# Go build output
/out/
/archivarr
/archivarr.exe

# Node
web/node_modules/

# Built frontend assets (the committed dist/index.html placeholder is kept;
# real build output is regenerated by `npm run build` / Docker).
web/dist/assets/

# Local runtime data
/config/
/local/
*.db
*.db-wal
*.db-shm
*.log

# Private / local-only docs (kept on disk, never published)
DESIGN.md

# Branding build tooling (assets it produces live in web/public/)
.branding-tmp/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code
.claude/
**/.claude/